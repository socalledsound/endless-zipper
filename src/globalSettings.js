export const center = window.innerWidth/2;
export const offsetY = window.innerHeight/7;
export const lineHeight = 75;
export const font = '50px arial black'
const centerOffset = 85
export const initLeft = window.innerWidth/2 - centerOffset
// export const initTop = minTopVal - (circleSize * 2) - 100
export const initTop = window.innerHeight/7
// const initX = window.innerHeight/7
// const initY = 200
const circleSize = 100
const controlCircleColor = '#fff0ff'

export const controlCircle = {
    x: 100,
    y: 200,
    size: circleSize,
    color: controlCircleColor
}
